apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: workhub
  labels:
    app: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
      protocol: TCP
