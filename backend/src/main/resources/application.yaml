server:
  port: 8080

spring:
  application:
    name: workhub-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/workhub
    username: workhub
    password: workhub
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: dGhpcyBpcyBhIHZlcnkgc2VjdXJlIHNlY3JldCBrZXkgZm9yIHdvcmtodWIgand0
  expiration: 86400000 # 1 day in milliseconds

app:
  seed:
    tenant:
      name: Acme Corp
      plan: FREE
    admin:
      email: admin@acme.com
      password: password123
    user:
      email: user@acme.com
      password: password123
